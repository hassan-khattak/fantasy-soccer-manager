# Copy to .env and set EXPO_PUBLIC_API_URL to your API server address.
# For local Docker: use your machine's LAN IP, not localhost (Expo Go runs on a physical device).

EXPO_PUBLIC_API_URL=http://192.168.x.x:3000/api/v1
