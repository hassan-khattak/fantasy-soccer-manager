# Copy to .env and fill in values for local development outside Docker.
# When running via docker-compose, these are already set in docker-compose.yml.

DATABASE_URL=postgresql://postgres:password@localhost:5432/fantasy_development
DEVISE_JWT_SECRET_KEY=change_me_to_a_long_random_string_in_production
RAILS_ENV=development
